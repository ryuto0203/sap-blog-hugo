[build]
  command = "hugo --minify --buildFuture"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.159.1"

# mainブランチのみ本番デプロイ
[context.production]
  command = "hugo --minify --buildFuture"

# developブランチはデプロイしない（プレビューのみ）
[context.branch-deploy]
  command = "hugo --minify --buildDrafts --buildFuture"

[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
